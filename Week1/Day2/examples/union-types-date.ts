//Choosing a delivery day

type Day = 'MON' | 'TUE' | 'WEDS' | 'THUR' | 'FRI'

const chooseDeliveryDay = (day: Day) => {
  // sort delivery
}

chooseDeliveryDay('MON') // OK
chooseDeliveryDay('SATURDAY') // Not OK

export {}
